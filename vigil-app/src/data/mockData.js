// ============================================================
// VigilOS — Fleet Data
// NO MOCK DATA — All data comes from real devices via backend API
// ============================================================

// Vehicles — empty until real ESP devices connect and send telemetry
export const INITIAL_VEHICLES = [];

// Incident logs — populated from real panic button triggers
export const INCIDENT_LOGS = [];

// Traffic analytics — populated from real telemetry data
export const TRAFFIC_DATA = [];

// Geofence zones (operational areas as polygons) — configure via backend
export const GEOFENCES = [];

// Corridor routes with buffer zones — configure via backend
export const CORRIDORS = [];
